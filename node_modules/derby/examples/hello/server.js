var express, hello, server;
express = require('express');
hello = require('./hello');
server = express.createServer().use(express.static(__dirname + '/public')).use(hello.router());
hello.createStore({
  listen: server
});
server.listen(3000);